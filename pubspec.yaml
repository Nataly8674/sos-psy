name: piloto
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  flutter_localizations:
    sdk: flutter
  collection: ^1.19.1
  shared_preferences: ^2.5.3
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# APENAS UM BLOCO FLUTTER
flutter:
  uses-material-design: true
  
  # Adicione suas imagens aqui
  assets:
    - assets/images/